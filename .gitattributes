# Normalize text files while leaving detected binary files untouched.
* text=auto

# Platform-specific script line endings.
*.sh text eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf

# Use Git's built-in C# diff driver for clearer hunk headers.
*.cs text diff=csharp
